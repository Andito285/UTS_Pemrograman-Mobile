name: cinema_app
description: A cinema booking app with geolocation.
publish_to: 'none'
version: 1.0.0+1

# ✅ Tambahkan baris ini!
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^10.1.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true